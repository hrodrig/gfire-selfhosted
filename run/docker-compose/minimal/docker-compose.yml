# json-file log rotation (avoids unbounded container log growth on disk).
x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"

services:
  postgres:
    # PG 18+: official image VOLUME is /var/lib/postgresql (not .../data).
    # Do not reuse a PG16 data directory here — dump/restore or fresh init.
    # Bookworm (not Alpine): fewer High CVEs in typical grype/docker scans.
    image: postgres:18.4-bookworm
    container_name: gfire-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${GFIRE_POSTGRES_DB:-gfire}
      POSTGRES_USER: ${GFIRE_POSTGRES_USER:-gfire}
      POSTGRES_PASSWORD: ${GFIRE_POSTGRES_PASSWORD:?set GFIRE_POSTGRES_PASSWORD in ${GFIRE_STACK_HOST_DATA}/.env}
    ports:
      - "${GFIRE_POSTGRES_HOST_PORT:-5432}:5432"
    volumes:
      # Durable DB files live under GFIRE_STACK_HOST_DATA (outside the git clone on servers).
      - ${GFIRE_STACK_HOST_DATA:?set GFIRE_STACK_HOST_DATA in .env}/postgres:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${GFIRE_POSTGRES_USER:-gfire} -d ${GFIRE_POSTGRES_DB:-gfire}"]
      interval: 3s
      timeout: 3s
      retries: 10
    logging: *default-logging

  gfire:
    # Full ref overrides GFIRE_VERSION+GHCR: set GFIRE_IMAGE in ${GFIRE_STACK_HOST_DATA}/.env.
    image: ${GFIRE_IMAGE:-ghcr.io/hrodrig/gfire:${GFIRE_VERSION:-v1.0.3}}
    container_name: gfire
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    # Distroless image default CMD is "version"; start the API + workers.
    command: ["server"]
    ports:
      - "${GFIRE_HOST_PORT:-8080}:8080"
    environment:
      GFIRE_SERVER_HOST: "0.0.0.0"
      GFIRE_SERVER_PORT: "8080"
      GFIRE_STORAGE_BACKEND: ${GFIRE_STORAGE_BACKEND:-postgres}
      GFIRE_STORAGE_POSTGRES_DSN: ${GFIRE_STORAGE_POSTGRES_DSN:?set GFIRE_STORAGE_POSTGRES_DSN in ${GFIRE_STACK_HOST_DATA}/.env}
      GFIRE_AUTH_ENABLED: ${GFIRE_AUTH_ENABLED:-false}
      GFIRE_AUTH_TOKEN: ${GFIRE_AUTH_TOKEN:-}
      GFIRE_STORAGE_REDIS_ADDR: ${GFIRE_STORAGE_REDIS_ADDR:-}
      GFIRE_STORAGE_REDIS_PASSWORD: ${GFIRE_STORAGE_REDIS_PASSWORD:-}
      GFIRE_STORAGE_REDIS_DB: ${GFIRE_STORAGE_REDIS_DB:-0}
    # Optional HOST_DATA config (GSH-011). See ../gfire.example.yaml.
    # Required for env-only postgres when using gfire v1.0.0 (nested GFIRE_* ignored).
    # volumes:
    #   - ${GFIRE_STACK_HOST_DATA}/gfire.yaml:/app/gfire.yaml:ro
    # command: ["server", "--config", "/app/gfire.yaml"]
    logging: *default-logging

  redis:
    profiles: ["redis"]
    image: redis:7-alpine
    container_name: gfire-redis
    restart: unless-stopped
    ports:
      - "${GFIRE_REDIS_HOST_PORT:-6379}:6379"
    volumes:
      - ${GFIRE_STACK_HOST_DATA:?set GFIRE_STACK_HOST_DATA in .env}/redis:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 3s
      timeout: 3s
      retries: 5
    logging: *default-logging

  valkey:
    profiles: ["valkey"]
    image: valkey/valkey:8-alpine
    container_name: gfire-valkey
    restart: unless-stopped
    ports:
      - "${GFIRE_VALKEY_HOST_PORT:-6380}:6379"
    volumes:
      - ${GFIRE_STACK_HOST_DATA:?set GFIRE_STACK_HOST_DATA in .env}/valkey:/data
    healthcheck:
      test: ["CMD", "valkey-cli", "ping"]
      interval: 3s
      timeout: 3s
      retries: 5
    logging: *default-logging
